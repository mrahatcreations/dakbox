server {
  listen 80;
  location / {
    root /usr/share/nginx/html;
    try_files $uri $uri/ /index.html;
  }
  location /api/ { proxy_pass http://stalwart:8080/api/; }
  location /jmap/ { proxy_pass http://stalwart:8080/jmap/; }
}
